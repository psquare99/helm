import 'package:flutter/material.dart';

/// Minimal, Calm Architectural Palette for Project Manager
/// Inspired by Swiss precision instruments, clean studio paper, and restrained telemetry.
class CommandColors {
  // Backgrounds & Surfaces (Light & Calm)
  static const Color background = Color(0xFFF8FAFC);    // Porcelain canvas
  static const Color surfaceBase = Color(0xFFFFFFFF);   // Pure white deck
  static const Color surfaceCard = Color(0xFFFFFFFF);   // White instrument panel
  static const Color surfaceRaised = Color(0xFFF1F5F9); // Light slate neutral
  static const Color surfaceHover = Color(0xFFE2E8F0);  // Subtle hover tone

  // Structural Precision Borders (Hairline architectural rules)
  static const Color borderSubtle = Color(0xFFE2E8F0);
  static const Color borderMedium = Color(0xFFCBD5E1);
  static const Color borderActive = Color(0xFF94A3B8);

  // Typography (Ink & Graphite)
  static const Color textPrimary = Color(0xFF0F172A);   // Deep ink slate
  static const Color textSecondary = Color(0xFF475569); // Calm graphite
  static const Color textMuted = Color(0xFF64748B);     // Technical telemetry label
  static const Color textDisabled = Color(0xFF94A3B8);  // Empty / placeholder state

  // Restrained Telemetry Signals (Calm, legible contrast)
  static const Color signalEmerald = Color(0xFF059669);
  static const Color signalEmeraldSoft = Color(0xFFECFDF5);
  static const Color signalEmeraldBorder = Color(0xFFA7F3D0);
  static const Color signalEmeraldGlow = Color(0x22059669);

  static const Color signalAmber = Color(0xFFD97706);
  static const Color signalAmberSoft = Color(0xFFFFFBEB);
  static const Color signalAmberBorder = Color(0xFFFDE68A);
  static const Color signalAmberGlow = Color(0x22D97706);

  static const Color signalCoral = Color(0xFFDC2626);
  static const Color signalCoralSoft = Color(0xFFFEF2F2);
  static const Color signalCoralBorder = Color(0xFFFECACA);
  static const Color signalCoralGlow = Color(0x22DC2626);

  static const Color signalIce = Color(0xFF0284C7);
  static const Color signalIceSoft = Color(0xFFF0F9FF);
  static const Color signalIceBorder = Color(0xFFBAE6FD);
  static const Color signalIceGlow = Color(0x220284C7);

  static const Color signalViolet = Color(0xFF7C3AED);
  static const Color signalVioletSoft = Color(0xFFF5F3FF);

  static const Color signalSlate = Color(0xFF64748B);
  static const Color signalSlateSoft = Color(0xFFF1F5F9);

  // Accent
  static const Color accent = Color(0xFF0284C7);
}
